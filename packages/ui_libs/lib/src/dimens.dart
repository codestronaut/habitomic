import 'package:flutter/material.dart';

class Dimens {
  /// Used for padding and margin
  static const tiny = EdgeInsets.all(4.0);
  static const small = EdgeInsets.all(8.0);
  static const normal = EdgeInsets.all(16.0);
  static const medium = EdgeInsets.all(24.0);
  static const large = EdgeInsets.all(32.0);
  static const veryLarge = EdgeInsets.all(48.0);
}
